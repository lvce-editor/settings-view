import { test } from '@jest/globals'

test.todo('diff returns RenderItems when states are different objects with same values')

test.todo('diff returns both RenderFocus and RenderItems when focus values differ')

test.todo('diff returns RenderItems when states are different objects')

test.todo('diff returns both RenderFocus and RenderItems when both conditions are met')

test.todo('diff returns RenderFocus when only focus differs')

test.todo('diff works with negative focus values')

test.todo('diff works with zero focus values')

test.todo('diff returns readonly array')

test.todo('diff returns RenderSettingValues when filteredItems change')
